[project]
name = "lista-tareas"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bcrypt>=5.0.0",
    "fastapi[all]>=0.136.1",
    "sqlalchemy>=2.0.49",
]
